struct string{
    char* arr;
    i32 len;
}

fn len(charp){
    type _ = enable_if(charp == @char);
    let cl = 0;
    while (charp[cl]!='\0'){

    }


}

fn __init__(sptr, char*){
    
    

}


fn main() -> i32{

    return 0;
}
