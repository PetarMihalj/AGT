fn outnl(i){
    out(i);
    out('\n');
}

struct rect<T>{
    let a = T;
    let b = T;
}

fn main() -> i32{
    let a = in<i32>();
    let b = in<i32>();

    type irect = rect<i32>;
    let rect_heap = heap_object<irect>(a,b);

    outnl(rect_heap!.a);
    outnl(rect_heap!.b);
    heap_free(rect_heap);
    outnl(rect_heap!.a);
    outnl(rect_heap!.b);
}
